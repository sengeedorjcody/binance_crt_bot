# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Code Editor for React Inspector (optional)
# Supported: code, cursor, code-insiders, webstorm, idea, subl, atom
# Default: code (VS Code)
# LAUNCH_EDITOR=code
# CODE_EDITOR=cursor
